{{ define "exec" }}
{{ template "header" . }}

{{ template "navbar" . }}

<section class="section">
	<div class="container">
		<div class="columns is-centered content">
			<div class="column is-7">
				<h2 class="subtitle">Where exec things go</h2>
			</div>
		</div>
	</div>
</section>


{{ template "footer" . }}
{{ end }}
