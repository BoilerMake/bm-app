# `migrations/`
Changes to the database have to be made as migrations so that the way we store data is portable and consistent. Each migration has an up (changes needed to get database to current state) and down (changes needed to bring database back to previous state) section.
